apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: checkoutservice-monitor
spec:
  selector:
    matchLabels:
      app: checkoutservice

  endpoints:
  - port: metrics
    interval: 5s
    path: /metrics