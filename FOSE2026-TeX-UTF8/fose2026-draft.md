# FOSE2026 ライブ論文 下書き

- **投稿区分**: ライブ論文(2ページ以内 / ショートプレゼン5分 / 査読あり)
- **投稿先**: EasyChair(PDF)。テンプレートは FOSE2026 配布のもの(pLaTeX)
- **締切**: 2026-09-14(月) 17:00 / 採否 9-18 / カメラレディ 9-25
- **CFP**: https://fose.jssst.or.jp/fose2026/cfp.html
- 作成 2026-09-09 / **最終更新 2026-09-18**
- ★**正は TeX 原稿 `FOSE2026-TeX-UTF8/fose2026.tex`。**この md は現状のスナップショット

## ★★ 提出済み (2026-09-14)

- 2026-09-14 に `fose2026.tex` (2 ページ、太字なし、§2 モデル定義・HPA 設定・localhost 実装・softirq 根拠・affinity 割合を反映した版) をビルドした PDF を EasyChair に提出した
- 提出版のスナップショット: `submitted-20260914/` (tex / bib / pdf、MD5 は `fose2026-data-provenance.md` §1)
- 論文の全数値の出所・計算スクリプト・再現状況: **`fose2026-data-provenance.md`** (表1・§3.1・§3 affinity・§5 割合は再計算で一致。§5 の 5–9%/28–48% は前セッションの awk 計算を Python に移植し一致を確認)
- 以降の編集はカメラレディ (9/25 17:00) 向け。提出版と差分を取るときは `submitted-20260914/fose2026.tex` と比較する

### ★カメラレディで判断すること (2026-09-18 記録)

1. **§5 の「5–9% / 28–48%」の再計算値への差し替え**。元の計算はウォームアップ中のサンプルも需要の平均に含めており、本文「180 秒のウォームアップ後 240 秒を計測」と食い違う。計測窓のみで計算し直すと **一定組 7–11% / 反転組 19–42%** (`summer2026/requests_shift_search.py --window`)。結論は変わらない。詳細と原因は `fose2026-data-provenance.md` §3.6。→ **未決**
2. 複数の Claude サブエージェントに役割 (K8s 技術 / 実験方法論 / 論理・構成) を分けて第三者査読させる案は提示済み。今回は見送り (ユーザ判断)。必要になったら現行 tex を渡して実行する
3. 査読 120 項目のうち未対応の「高」: 総当たり探索の方法と解の定義 (1 と併せて書けば対応可) / 表1の 19/20 の理由 (frontcart cycle1 view300 の欠測) / 6組を選んだ理由 (7組から catalog+checkout を除外。provenance §3.5 に記載)
4. **§3.1 予備実験の warm/meas (90 s / 120 s) が本文に無い** (§4 の 180/240 s とは別)。前セッションの記録で判明。1 行で足せる: 「各点は 90 秒のウォームアップ後 120 秒を計測」
5. 前セッションの未保存計算の一覧 (下の「## 前セッションの未保存の計算・判断」) は 2026-09-18 に検証し provenance §3.2 / §3.5 / §3.6 / §3.7b に統合済。`fixed4_summary.py` の docstring (240→120 s) を訂正、`table1_mismatch.py` に catalog+checkout の参考行を追加

---

## 題目

**Kubernetes HPA 環境下におけるコンテナ同居の効果と選択条件**

英題: Effects and Selection Conditions of Container Colocation under Kubernetes HPA

---

## 概要

複数サービスを1つの Pod に同居させる**部分集約**は通信コストを下げるが、Kubernetes では HPA が Deployment 単位で台数を決めるため、集約すると**スケールの粒度が粗くなり**予約資源が増える。本稿では Online Boutique を用いた実機実験により、集約がスループットと通信コストを改善する一方、この損失の大きさが**通信量**と**ワークロード成分**という独立な2軸で決まることを示す。

---

## 1. はじめに

マイクロサービスを細分化すると、サービス間の呼び出しがネットワークスタックを経由するため、通信処理のオーバヘッドが生じる。直接通信するサービスを1つの Pod に同居させ、通信を `localhost` 経由にする**部分集約**はこれを削減する。Wickramanayaka ら[1]は Docker Swarm 環境で、通信量の多いコンテナの適正配置・統合を行い通信量が最大58.5%減少することを示した。

この効果を、実運用のデファクトスタンダードである Kubernetes に取り入れたい。しかし中核機能である HPA は Deployment 単位で台数を決めるため、集約すると**スケールの粒度が粗くなる**という問題が生じる。本稿ではこの環境での効果を確認したうえで、相手選びに必要な判断軸を示す。

> ★2026-09-12 に書き換え。旧版は「先行研究は Kubernetes のオートスケール環境を対象としていない」という**欠落の指摘**だったが、(a) Docker Swarm にも第三者製オートスケーラはあるので Kubernetes を使う理由にならない、(b)「まだ誰もやっていない」は動機として弱い、という理由でユーザ判断により**「デファクトスタンダードに取り入れたい → しかし HPA と噛み合わない」**という筋に変更した。

---

## 2. 部分集約とオートスケーラの干渉

HPA は Deployment 単位で台数を決めるため、同居したコンテナは**同じ台数でしか複製できず**、その台数は各コンテナが必要とする台数の最大値になる。サービス s の枠 (requests) を r(s)、単独で必要な台数を n(s)、同居した Pod の台数を N = max n(s) とすると、余分に確保される資源 Δ は

```
    Δ = Σ ( N − n(s) ) × r(s)        … 式(1)
        s
```

となる。**損は「引きずられた側の枠」に比例**し、必要台数が揃えば Δ = 0 である。以降この Δ を**粒度損**と呼ぶ。

---

## 3. 実験環境

対象は Online Boutique[2] (11サービス、gRPC 通信) である。MicroK8s (単一ノード、16コア) 上にデプロイし、k6 を用いて負荷を与えた。

負荷は2種類の利用者を模した。**buy** は閲覧・カート追加・購入を、**view** は閲覧のみを1周とする。**view は checkout・payment・shipping・email に一切到達しない**。以降、1秒あたり x 周のレートで与える負荷を buy(x)・view(x) と表す。

**表1 2種類の利用者が到達するサービス**

| 利用者 | 到達するサービス |
|---|---|
| buy | 全10サービス |
| view | frontend, productcatalog, cart, currency, recommendation, ad |

評価指標は、粒度損として**予約枠** (稼働 Pod の requests 合計)、通信コストとして **softirq の CPU 時間**を用いた。

### 3.1 予備実験: Kubernetes 環境での効果

分離 (1 Pod 1 サービス)、frontend・recommendation・productcatalog を同居させた **front3**、全サービスを同居させた **mega** の3構成を比較した。台数は4で固定して HPA を切り、全コンテナの枠を150mに揃えたので、予約資源はどの構成も 6.3 コアで一致する (3回反復、buy のみ)。

buy(150)〜buy(300) ではいずれも目標レートを達成し、取りこぼしと失敗は0であった。その条件で1周あたりの softirq は分離に対し front3 で 17〜23%、mega で 43〜51% 少なく、**同居させるほど通信コストが下がる**。応答時間は buy(300) で分離だけが不安定になり (p50 が 23〜79 ms、同居は 16〜21 ms)、過負荷 (buy(500)) では達成レートが分離 348 周/秒 に対し front3 425、mega 435 と、**同じ予約資源でより多くを処理**できた。

> ★2026-09-13 に差し替え。旧版は7月の locust(**閉ループ**)で「rps +10.2%」としていたが、**閉ループのスループットは応答時間の言い換え**にすぎない(normal の p50 340ms 対 集約 37ms → 同じユーザ数でも周回数が減るだけ)。新データ(k6 開ループ・台数固定・予約一致)に差し替えたことで、§4 と §5 の測定系不一致も解消した。

---

## 4. 本実験

枠 (requests) はマニフェストの既定値ではなく、実測から適正化した値を用いる。各サービスを1台に固定し HPA を切った状態で負荷を6段階 (10〜150周/秒) 与え、CPU 使用量を負荷の1次式として最小二乗法で近似し、

```
    r = ( c0 + c1 × k ) / 0.7,   k = 100      … 式(2)
```

とした。c0 はアイドル時の CPU 使用量、c1 は1リクエストあたりの CPU 時間、k は基準負荷 [周/秒] である。これにより、理想的には全サービスが同じ負荷で目標利用率に到達し、必要台数が一致する。その上で、buy(100) を一定に保ちつつ view を4段階(0・100・200・250周/秒)で変える実験を行った。

### 4.1 ワークロード成分が異なると損は消せない

buy のみを与えた場合にも粒度損は生じたが、これは email の利用率が増設ラインをわずかに超えた (83%) ためで、**枠が7%不足していただけ**であり、buy(150) では両者とも2台となって消える(表2の上2行)。すなわち**枠の測定精度に起因する損は除去できる**。

これに対し view(0) を view(300) にすると、**frontend の需要は2.5倍**になるのに対し **email は1.2倍**にしかならない(表2の1行目と3行目)。

**表2 条件による粒度損の変化 (FE+email、いずれも3/3で一致)**

| buy [周/秒] | view [周/秒] | 必要台数 FE/email | 引きずられる側 | 粒度損 [mCPU] |
|---|---|---|---|---|
| 100 | 0 | **1** / **2** | FE (1077 m) | **+1077** |
| 150 | 0 | 2 / 2 | — | **0** |
| 100 | 300 | **3** / 2 | email (119 m) | **+119** |

**同じ組み合わせでありながら、引きずる側と引きずられる側が入れ替わり、損が9倍変化した**。損の比 1077 ÷ 119 = 9.05 は枠の比と一致し、式(1)が両方向で成立している。

枠は各サービスに1つしか与えられないのに対し、需要の比はワークロードの混合比によって変化するため、**2つの条件で同時に損をゼロにする枠は存在しない**。すなわちこの損は枠の調整では原理的に除去できない。

ただし成分が異なれば必ず損が出るわけではなく、必要台数が一致した際の粒度損はゼロであった。

> ★旧版では 5.1「枠の調整で消える損」/ 5.2「枠の調整では消えない損」の2節だったが、分量の都合で1節に統合。また「同時にゼロにする枠が存在しない」ことの不等式による導出(93/r ≤ 0.77 → r ≥ 121、113/(2r) > 0.77 → r < 73)はユーザ判断で削除した。

---

## 5. 考察

便益は通信量で決まり、枠を3倍変えても softirq の削減率は 16.4%・16.2% とほぼ変わらない。一方、損は成分で決まる。この独立な2軸から、避けるべきは**「通信量が多く、かつ成分が異なる」組み合わせ**となる。しかし実測では太い通信の両端はいずれも同一成分で、成分をまたぐ通信は最大 2.6% であり、本アプリケーションに**この組み合わせは存在しなかった**。便益の大きい相手を選ぶ限り、粒度損の危険は踏まないことになる。

---

## 6. 今後の課題

成分を跨ぐアプリケーションでの実行。(★本文が書きかけ)

また本実験は単一ノードで行っており、ノードをまたぐ通信 (overlay ネットワーク) を含まない。先行研究[1]が示した効果の大部分は overlay の有無に由来するため、マルチノード環境では便益がさらに大きくなると予想される。

---

## 参考文献

[1] I. Wickramanayaka, C. Keppitiyagama, K. Thilakarathna: Communication-Affinity Aware Colocation and Merging of Containers, *International Journal of Advances in ICT for Emerging Regions*, 15(3), 2022.

[2] Google Cloud Platform: Online Boutique, GitHub `GoogleCloudPlatform/microservices-demo`, 2025.

> ★NotNets [Alvaro 2024] の引用は削除済み(配置の研究ではないので「オートスケール環境を対象としていない」という限界指摘が当たらない)。**bib エントリ `alvaro2024` は `fose2026.bib` に残してある**ので復活は容易。復活させるなら先行研究の段落ではなく第1段落(動機)に置く。

---

# 執筆メモ(提出時は削除)

## ★★ 追加実験の指示: 全構成を5サイクルに統一 (2026-09-13 ユーザ決定)

### 方針
表2 の7構成はサイクル数がバラバラ(frontcatalog のみ6、他は3)なので、**全構成5サイクル**に揃える。
**枠の適正化と実験設計は、mix / mix2 の対応するものと必ず同じにすること。** 片方でも違うと既存データと合算できず、全部やり直しになる。

### 現状と不足

| アーム | 出所 | 現在 | 追加 |
|---|---|---|---|
| frontcatalog | mix | 6 (cyc1-6) | **不要**(cyc1-5 を使う) |
| frontreco | mix | 3 | +2 |
| frontcart | mix | 3 | +2 |
| normal | mix | 3 | +2 |
| frontemail | mix2 | 3 | +2 |
| frontcheckout | mix2 | 3 | +2 |
| checkoutemail | mix2 | 3 | +2 |
| catalogcheckout | mix2 | 3 | +2 |
| normal | mix2 | 3 | +2 |

追加 64点 (mix 側24点 + mix2 側40点)。**1点 7分45秒 → 約8時間15分。**

### 実行コマンド

**mix 側**(適正化対象 = frontend / productcatalog / **recommendation** / **cart**)

```sh
cd /home/mizuki/ダウンロード/msa
DIR=km2/experiments/summer2026
ARMS="normal frontreco frontcart" \
RATES="100" BROWSE_RATE="0 100 200 300" \
CYCLES=2 CYC_START=4 \
RIGHTSIZE=1 RS_SERVICES="frontend productcatalogservice recommendationservice cartservice" \
RS_SCALE=1 LIMIT_X=0 LIMIT_SCALE=0 \
HPA_TARGET=70 HPA_MIN=1 HPA_MAX=8 SU_WIN=30 SD_WIN=60 PRESCALE=1 \
WARM=180 MEAS=240 SAMPLE=15 PRE_VUS=600 MAX_VUS=4000 \
CSV=$DIR/results-mix.csv TL=$DIR/timeline-mix.csv EV=$DIR/events-mix.csv LOG=$DIR/mix.log \
bash $DIR/bundle-vs-loss2.sh
```

**mix2 側**(適正化対象 = frontend / productcatalog / **checkout** / **email**)

```sh
ARMS="normal frontemail frontcheckout checkoutemail catalogcheckout" \
RATES="100" BROWSE_RATE="0 100 200 300" \
CYCLES=2 CYC_START=4 \
RIGHTSIZE=1 RS_SERVICES="frontend productcatalogservice checkoutservice emailservice" \
RS_SCALE=1 LIMIT_X=0 LIMIT_SCALE=0 \
HPA_TARGET=70 HPA_MIN=1 HPA_MAX=8 SU_WIN=30 SD_WIN=60 PRESCALE=1 \
WARM=180 MEAS=240 SAMPLE=15 PRE_VUS=600 MAX_VUS=4000 \
CSV=$DIR/results-mix2.csv TL=$DIR/timeline-mix2.csv EV=$DIR/events-mix2.csv LOG=$DIR/mix2.log \
bash $DIR/bundle-vs-loss2.sh
```

### ★絶対に変えてはいけない設定(既存ログから確認済み)

| 項目 | mix (9/2・9/3) | mix2 (9/7) |
|---|---|---|
| 購入レート | `RATES=100` | 同じ |
| 閲覧レート | `BROWSE_RATE="0 100 200 300"` | 同じ |
| HPA | 目標70% / 1-8台 / 増30s・減60s | 同じ |
| 開始台数 | **1** (`PRESCALE=1`) | 同じ |
| ウォームアップ / 計測 | 180s / 240s | 同じ |
| 採取間隔 | 15s | 同じ |
| `RIGHTSIZE` | 1 | 1 |
| `RS_SCALE` / `LIMIT_X` / `LIMIT_SCALE` | 1 / 0 / 0 | 同じ |
| **`RS_SERVICES`** | frontend productcatalogservice **recommendationservice cartservice** | frontend productcatalogservice **checkoutservice emailservice** |
| 適正化テーブル | `summer2026/rightsize-requests.csv` | 同じ |

**`RS_SERVICES` だけが両者で異なる。** ここを取り違えると既存データと混ぜられない。

`CYC_START=4` にすること(既存が cyc1-3 のため)。mix.csv には frontcatalog / frontrecocatalog / frontrecocartcatalog の cyc4-6 が既にあるが、アームが違うので衝突しない。

### 完了後にやること
1. 全構成 cyc1-5 で再集計し、表2 の「需要の伸び」と「粒度損」を更新
2. §4 冒頭の `(3サイクル)` を `(5サイクル)` に
3. 表2 の脚注から「frontend+catalog のみ6サイクル」を削除

### ★未解決のまま残る問題(サイクルを増やしても直らない)
mix と mix2 は **`RS_SERVICES` が違う**ため、同じサービスでも需要の伸びの値がずれる(cart: mix 1.45倍 / mix2 1.94倍)。表2 の**行間で伸びを比較することは厳密にはできない**。
直すには7構成を1本の実験・単一の枠設定で回す必要があり、3サイクルでも約12時間25分。締切には間に合わないため見送り。
論文には「枠の適正化は同居対象に限るため、実験ごとに適正化したサービスが異なる」と書いて対応する(§4 冒頭の方針と整合)。

---

## 前セッションの未保存の計算・判断 (セッション 8798bd8a、2026-09-09〜09-14、記録 2026-09-18)

論文執筆セッションで awk ワンライナー等で出した計算と、そこから下した判断のうち、
リポジトリのスクリプト・`fose2026-data-provenance.md`・`EXPERIMENTS.md` に**残っていないもの**。
出所はセッション記録 `~/.claude/projects/c--msa/8798bd8a-7008-4d8d-b864-ecd785519568.jsonl` の行番号 (L番号)。
時刻は記録の UTC (JST は +9 時間)。

★共通の注意: 以下の需要 (利用率×台数) を使う計算 (1〜6, 9, 10) は、provenance §3.6 と同じく
**ウォームアップ区間も含めた全サンプルの平均**で計算している (計測窓 180–420 s に限定していない)。
計測窓のみで計算し直すと値は変わる (結論は変わらない見込みだが未確認)。

### A. 現在の本文の記述に効いている計算 (未保存)

1. **checkout+email の片側 5% 調整で全点一致** (L2889, L2916 / 09-13 17:32–17:36)
   - (1) email の予約枠を 119 m → 125 m (×1.05) にすると checkout と email の必要台数が全 12 点 (3 サイクル×4 view) で一致する
   - (2) `summer2026/timeline-mix2.csv` の arm=normal, cycle 1–3, container = checkoutservice / emailservice
   - (3) 需要 D = hpa_util_pct/100 × current_replicas を全サンプル平均。必要台数 = 1 (D≤0.77) / 2 (D≤1.54) / 3。email は D×119/125 に換算して checkout の台数と比較
   - (4) 旧 §4.2 「view(0) の checkout+email は枠を約 5% 増やすだけで損が消える」→ 具体的すぎるとして削除。現在は §5 の「一定な 3 組は 5–9%」の checkout+email 5% (両側 ×0.98/×1.05、`requests_shift_search.py`) に吸収されている

2. **片側だけの枠探索** (L3157 / 09-13 20:16)
   - (1) 片方のサービスの枠だけを k 倍 (0.30–3.00、0.02 刻み) して全 20 点で必要台数が一致する k があるか。**checkout+email のみ解あり (email ×1.06)**、他 5 組 (frontend+reco/catalog/cart/checkout/email) は解なし
   - (2) `timeline-mix.csv`, `timeline-mix2.csv` の normal 行、cycle 1–5
   - (3) 需要は全サンプル平均、必要台数 = max(1, ceil(D/k/0.77))
   - (4) 旧 §4 の「frontend+checkout は枠を少し動かせば解消する」を削除した根拠。片側では向きが一定な frontend+catalog/reco も消せないため、両側を同時に動かす探索 (`requests_shift_search.py`) に切り替えた。※スクリプトの `--variant` は「片側・θ=0.70・計測窓」で条件が違う

3. **両側探索・範囲無制限** (L3162 / 09-13 20:16)
   - (1) 両方の枠を 0.30–3.00 (0.02 刻み) で動かすと**全 6 組に解がある**: frontend+reco 2076 通り (例 ×0.76/×0.74)、frontend+catalog 2890、frontend+cart 2508 (例 ×0.72/×0.64)、checkout+email 8988、frontend+checkout 3672 (例 ×0.80/×0.50)、frontend+email 3628 (例 ×1.60/×1.06)
   - (2)(3) 2 と同じ
   - (4) 一時書いた「全帯で損をゼロにする枠は存在しない」が誤りと分かり、§5 を「事実上予約枠では消せない」に書き換えた根拠

4. **±20% に限った探索** (L3173 / 09-13 20:17)
   - (1) 両側を 0.80–1.20 に限ると frontend+reco 6 通り (例 ×0.88/×0.84)、frontend+catalog 35 通り (例 ×1.08/×0.92)、checkout+email 30 通り (例 ×0.80/×0.80) に解があり、frontend+cart / frontend+checkout / frontend+email は解なし
   - (2)(3) 2 と同じ
   - (4) 一時「±20% 以内では解なし」と書いたが、20% という境界が恣意的 (ユーザ指摘) なので撤回し、「損を消せる最小のずらし」(L3212 → `requests_shift_search.py` に保存済) に置き換えた

5. **7 組目 catalog+checkout の食い違い (5 サイクル)** (L3028, L3044 / 09-13 19:40–19:43)
   - (1) catalog+checkout は 4/20 点で食い違い、向きは v0: catalog が 1 回、v100: checkout が 2 回、v300: checkout が 1 回 = **反転あり**。他 6 組の値は表1と同じ (3/20, 7/20, 10/19, 6/20, 9/20, 11/20)
   - (2) `results-mix.csv`, `results-mix2.csv` の normal 行 replicas 列、cycle 1–5
   - (3) `table1_mismatch.py` と同じ定義 (同居アームの行がある点だけ数える)
   - (4) 表1から catalog+checkout を外し「6 つの組み合わせ」とした (ユーザ指示 09-13)。`table1_mismatch.py` は 6 組だけなので、この 7 組目の値は未保存。★注意: メモリ `demand-mismatch-experiment` の「catalogcheckout は全帯で損ゼロ」は 3 サイクル時点 (2/12、v100 のみ) の値で、5 サイクルでは反転を含む
   - 6 組を選んだ理由 (査読で未対応の「高」): mix (frontcatalog/frontreco/frontcart) と mix2 (frontemail/frontcheckout/checkoutemail/catalogcheckout) にある 2 サービスの組 7 つから catalog+checkout を除いたもの。3〜4 サービス束ね (frontrecocatalog, frontrecocartcatalog) は対象外

6. **3 サイクル時点の損の点数** (L2755 / 09-13 17:10)
   - (1) frontcatalog 4/24、frontreco 1/12、frontcart 6/11、frontemail 7/12、frontcheckout 6/12、**checkoutemail 5/12** (v0: 3/3, v100: 2/3)、catalogcheckout 2/12
   - (2) `results-mix.csv` (cycle 1–6)、`results-mix2.csv` (cycle 1–3)
   - (3) 同じ (cycle, view) で組の一方の normal 台数が他方より小さければ「損あり」と数える
   - (4) 旧表2 の「損の点数」列。checkout+email を「損なし」と誤記していたのを 5/12 に訂正した根拠。5 サイクル完了後 (09-14 04:39) に表1へ置き換え

7. **飽和帯の点ごとの値と 6 サイクル限定** (L2555, L2572, L2599, L2603 / 09-13 16:01–16:17)
   - (1) cycle 1–6 のみ: buy(350) 分離 348.0±4.0 周/s (取りこぼし>0 が 3/6 回)、front3 350.0 (0/6)、mega 350.0 (0/6)。buy(400) 分離 361.7±26.8 (6/6)、front3 399.5±0.4 (5/6)、mega 399.9±0.2 (1/6)。buy(450) 分離 359.6±22.9、front3 439.5±5.5、mega 437.4±1.6 (各 6/6)。12 サイクル全部だと buy(350) 分離 344.6±9.4 (9/12)、buy(400) 分離 353.8±23.6
   - (2) `summer2026/results-fixed4b.csv` (実測 = 列 4、取りこぼし窓 = 列 25)
   - (3) 平均 ± 不偏標準偏差、範囲、取りこぼし窓>0 の回数
   - (4) §3.1 「同居が約 440 周/秒、分離 359.6±22.9」。**判断**: buy(350)/(400) の cycle 7–12 は 2026-09-10 17:26 JST の別走 (450 を含まない、`fixed4b.log` 415 行目) なので除外し、450 と一緒に測った cycle 1–6 に統一 (ユーザ指示「一緒に測った 6 サイクルにしてほしい」)。`fixed4_summary.py` は 450 の平均±SD だけを出し、350/400 の取りこぼし回数は未保存。旧版にあった 350/400 の記述 (「分離は 350 で取りこぼし」等) は 2 ページ制約で削除

8. **基本帯 softirq/周の計測時間** (L1668 / 09-12 01:10)
   - (1) softirq[ms/周] = softirq[コア秒] / (目標レート × **120 s**) × 1000 (例: 分離 buy(150) cyc1 = 54.0/(150×120)×1000 = 3.003)
   - (2) `results-fixed4.csv`。fixed4/fixed4b の実行は warm 90 s / meas 120 s (`fixed4.log` 1 行目)
   - (3) 上式のサイクル平均を分離と比較 → front3 −22.9/−23.1/−17.4/−18.3%、mega −50.6/−48.2/−46.5/−42.9%
   - (4) §3.1 「17–23%、43–51%」。★`fixed4_summary.py` の docstring は「計測 240 秒」と書いているが、`fixed4-all-points.csv` の softirq[ms/周] は 120 s で計算された値 (上の 3.003 と一致)。比率には影響しないが docstring は誤り。また §3.1 は予備実験の warm/meas (90/120 s) を本文に書いていない (§4 の 180/240 s とは別)

### B. 旧版で使ったが現在の本文にはない計算 (未保存、参考)

9. **需要の伸びと需要比の変化** (L1989, L2050, L2122, L2182 = 3 サイクル / L3033 = 5 サイクル)
   - (1) view(0)→view(300) の需要倍率 (5 サイクル、mix / mix2): frontend 2.33/2.33、catalog 2.14/2.09、reco 2.17/2.01、cart 1.58/1.86、checkout 1.23/1.23、email 1.14/1.09。需要比の変化率 (3 サイクル): frontend/catalog 1.11 倍、frontend/reco 1.09、frontend/cart 1.61、checkout/email 1.13、catalog/checkout 1.68、frontend/checkout 1.97、frontend/email 2.24 (0.85→1.90)
   - (2) `timeline-mix.csv`, `timeline-mix2.csv` normal 行
   - (3) 需要 = 利用率×台数 の全サンプル平均、view300 ÷ view0
   - (4) 旧 §4.1 「需要比が 2 倍超だと枠では直せない」と旧表2 の「需要の伸び」列 → ユーザ判断で削除 (「二値ではなく伸び率で語ると前提が崩れる」)。`cyc5_summary.py` は計測窓版の伸びを出すが「需要比の変化率」は未保存。mix と mix2 で cart の伸びが違う (1.58 vs 1.86) のは RS_SERVICES の違いによる (執筆メモ「未解決のまま残る問題」)

10. **需要の view に対する直線性** (L2006 / 09-13 08:21)
    - (1) v0→v300 を結ぶ直線から v100, v200 のズレは最大 +0.13 台分 (reco の v200)、他は ±0.11 以内 → 需要は view レートにほぼ比例
    - (2) `timeline-mix.csv` normal 行 (3 サイクル)
    - (3) e = D(v) − [D(0) + (D(300)−D(0))×v/300]
    - (4) 「伸び率で語れるか」の内部確認。論文には未使用

11. **台数の分布・平均による表の案** (L3060, L3082, L3087 / 09-13 19:46–19:55)
    - (1) サービス×view の台数分布 (例: frontend v200 = 2台×3, 3台×2) と平均 (frontend 1.0/2.0/2.4/2.9 等)
    - (2) `results-mix.csv`, `results-mix2.csv` normal 行 cycle 1–5
    - (4) 表の見せ方の候補として提示 → ユーザ却下 (「ペアごとにするべき」)、表1 (向き付き点数) に決定

12. **帰属方式の損/ゼロ/節約の点数** (L2144 / 09-13 08:48、3 サイクル)
    - (1) 同居 Pod の台数を各メンバーの normal 台数と比べ、frontcatalog 損 12/ゼロ 8/節約 4、frontreco 3/7/2、frontcart 5/5/1、frontrecocatalog 12/8/3、frontemail 8/3/1、frontcheckout 7/5/0、checkoutemail 5/7/0、catalogcheckout 2/10/0
    - (2) `results-mix.csv`, `results-mix2.csv`
    - (4) 旧 §4 の候補。`loss_attributed.py` / `cyc5_summary.py` の帰属方式とは集計単位が違う (点数 vs 枠 [m])。論文には未使用

13. **ratesweep2 の予約コア差** (L1884, L1889 / 09-13 08:04)
    - (1) view(0) で buy 100/150/200/250: frontemail +1.117/+0.432/−0.069/+0.333 コア、frontcheckout +0.349/+0.093/+0.045/+0.389、checkoutemail +0.321/+0.122/+0.145/+0.068、catalogcheckout 0/+0.157/−0.071/−0.041
    - (2) `summer2026/results-ratesweep2.csv` (列 38 = 予約コア)
    - (4) 旧表2 の buy(100)/buy(150) 列 → 「本実験は mix/mix2 のみ」(ユーザ決定 09-13) で削除。執筆メモ「数値の出所」の表はこの旧版のまま

14. **7 月の HPA 実験 (locust) の集計** (L810, L1617 / 09-10 06:54, 09-11 07:45)
    - (1) users=240、3 サイクル: f3perc rps 704.7±5.9 vs normal 639.2±32.5 (+10%)、softirq/req 0.632 vs 0.932 (−32%)、Pod 14.3 vs 18.3、fails は cyc3 のみ (f3avg 7 件・f3perc 112 件)
    - (2) `km2/experiments/results-hpapercont-allscale.csv`
    - (4) 初版 §3.1 予備実験に使用 → locust は閉ループでスループットが遅延の代理指標にしかならない (ユーザ指摘) ため、k6・4 台固定の fixed4 に差し替え。`latency-breakdown/replica-count-sweep.csv` も不採用 (執筆メモ「未解決の判断」2)

15. **probe5 (プローブ timeout 1→5 s の対照)** (L1832 / 09-13 07:53)
    - (1) front3 350: 350.0 周/s 取りこぼし 0、400: 399.9 / 11。normal 350: 335.3 / 1763、400: 361.6 / 4607 → 緩和しても分離は回復しない
    - (2) `summer2026/results-probe5.csv`
    - (4) 論文には未使用 (メモリ `fixed-replica-k6` に記録あり)

### C. 計算を伴わない判断 (根拠の所在)

- **view が到達するサービス** (§3): `km2/experiments/k6/checkout.js` の browse シナリオは `GET /product/{id}` のみ。`src/frontend/handlers.go` の productHandler が呼ぶのは getProduct (catalog)・getCurrencies/convertCurrency (currency)・getCart (cart)・getRecommendations (reco)・chooseAd (ad) の 5 つ → frontend を含め 6 サービス。shipping は /cart ページ (getShippingQuote) でしか呼ばれないので view では不到達 (L727, L765–772)
- **「10 サービス」** (§3): 公式 11 サービスから loadgenerator を除く (k6 で代替)。redis-cart はデータストアなので数えない (ユーザ確認)
- **需要 = 利用率 × 台数 の意味** (§2, §5): HPA の利用率は Pod ごとの CPU 使用量 ÷ requests の平均なので、利用率×台数 = 合計 CPU ÷ 予約枠 = 「枠何台分」。必要台数 = ceil(需要 / 0.77)、0.77 = 目標 70% × 許容幅 1.1 (増設ライン)
- **「1 台あたりが過大になる」→「実態から乖離する」** (§5): L3212 の解が frontend+email (email ×0.53)・frontend+checkout (checkout ×0.52) で枠を**縮める**方向だったため、「過大」は誤り
- **飽和帯・基本帯とも平均で語らない** (§3.1): 取りこぼしや p50 は分布が歪む (ユーザ指示「平均しないで」) → 本文は達成レートの平均±SD と「約 2 割上回った」のみ
- **予備実験の位置づけ**: fixed4 は `bundle-vs-loss2.sh` を `FIXED_REPLICAS=4 UNIFORM_REQ_M=150` で実行 = HPA を作らず全 Deployment 4 台固定 (`fixed4.log` 3, 6 行目で確認)。ログ 1 行目の「HPA=70%/1-8台」は既定値の表示で実際には未使用

## 分量

**現状3ページ。制限は2ページ。** 実測では本文をソースで約10行削れば入る。

未着手の削減候補(削減幅の大きい順):

| 候補 | 削減 | 失うもの |
|---|---|---|
| 式(2) と枠の適正化 | 約10行＋数式 | §4.1 の「枠が7%不足」の根拠。§4.1 を畳んだ今、用途はほぼこれだけ = **一番安い** |
| 予備実験 §3.1 | 約12行 | 「Kubernetes でも集約が効く」という土台 |
| §6 今後の課題 | 約7行 | 単独では足りない |

★注意: 日本語はソース1行(約40字)が組版で約2行になる。**表を削るより本文を削るほうが2倍効く**(表1+表2を両方消しても3ページのままだった)。

## 数値の出所

| 記述 | 出所 |
|---|---|
| §3.1 softirq −17〜23% / −43〜51%、p50、過負荷 348/425/435 | `km2/experiments/summer2026/results-fixed4.csv` (2026-09-09、k6、4台固定・HPA無し、150/200/250/300 各3サイクル + 500 を1回) |
| 表2 buy(100)/buy(150) | `summer2026/results-ratesweep2.csv` (実験9/7) |
| 表2 view(300) | `summer2026/results-mix2.csv` (実験9/7) |
| email 1台で83% | `summer2026/timeline-mix2.csv` / `timeline-ratesweep2.csv` |
| 枠を3倍変えても −16.4% / −16.2% | `summer2026/results-mix.csv` (9/2) と `results-scale3.csv` (9/4) |
| 成分をまたぐ通信は最大 2.6% | `km2/experiments/results-edge-pairs.csv` (2026-09-08、`ss -tin`) |

## 未解決の判断

1. **過負荷点 (348 / 425 / 435 周/秒) は 1 サイクルのみ**。softirq と p50 は3回反復だが、この3数字だけ n=1。本文では並べて書いてあるので「3回反復」と誤読されうる。「1回の観測」と明記するか反復を取る。
2. **便益の台数依存性が未検証**。本稿は台数4のみ。「スケールアウトしても効くのか」は査読で必ず問われる。
   - 既存の `latency-breakdown/replica-count-sweep.csv` は **使えない**(2026-07-14、locust 閉ループ、softirq 列なし、n=1、台数と性能が非単調: normal が 4台1015 / 3台688 / 2台930 / 1台681 rps)
   - 追加で回すなら: normal / f3perc × 台数 1・2・4 × buy(200) × 2サイクル = 12点、WARM 90s + MEAS 120s で**約1時間**。「削減率は台数に依らずほぼ一定」と言えれば一文で済む
   - 時間が無ければ §6 に「便益は台数4での測定であり、台数依存性の検証は今後の課題」と明記
3. **77% の由来が本文に無い**。§4.1 の「増設ラインをわずかに超えた (83%)」の増設ライン = **HPA 目標利用率70% × 許容幅1.1 = 77%**。HPA 設定の段落はユーザ判断で削除したので、由来だけをどこかに入れる(案: 「増設ライン (目標70%＋許容幅10%) である77%」= 行数増なし)。★ユーザが「別のところに入れる」として保留中
4. **§6 今後の課題が書きかけ**(「成分を跨ぐアプリケーションでの実行。」体言止め)
5. **著者の英語表記がプレースホルダ**(`Author Name` / `Graduate School, University`)

## 訂正確認場所

14. n(s)とrequestsの関係を整理する必要があります。 CPU使用率ベースHPAの場合，requestsを変更すると必要Replica数そのものが変化します。したがって，r(s)とn(s)を完全に独立した値のように扱うと分かりにくくなります。 ★一部対応: 利用率＝使用量÷予約枠は記載済。「requests を変えると n(s) も変わる」は未明示
16. 「同居するとスケールの粒度が粗くなる」という主張は分かりやすい一方，簡単な具体例があるとさらによいです。 例えば「Aは4台，Bは2台必要→同居すると4Podとなり，Bが2台分余分に複製される」という例を式の前に入れると，一読で粒度損を理解できます。
17. 現在の式だけでは，「向き」がなぜ重要なのかが直感的に分かりにくいです。 2サービスの例を使って，どちらが相手に引きずられるかによって余分に確保されるrequestsが変わることを説明すると，後半の実験に自然につながります。
18. 「相手選びに必要な判断軸を示す」という研究目的はよいですが，Research Questionにしてもよいです。 例えば「RQ1：HPA環境でも同居による通信処理削減効果は得られるか」「RQ2：粒度損を抑えられる同居相手にはどのような特徴があるか」とすると，3.1と4章がきれいに対応します。
19. 関連研究が1件しかなく，かなり少ないです。 コンテナ同居，マイクロサービス配置，Kubernetesのオートスケーリング，resource-aware placement等について少なくとも数件追加した方がよいです。
20. 先行研究との差分を明示してください。 現在は「Docker Swarmでは効果が確認されている→Kubernetesで確認する」となっています。 本研究の本質はKubernetesに移植したことよりも，HPAと同居との干渉による新たなトレードオフを分析したことだと思います。ここをもっと前面に出した方がよいです。
22. Online Boutiqueを採用した理由を一言入れるとよいです。 マイクロサービス間依存関係を持つ代表的ベンチマークであること等を説明すると，実験対象としての妥当性が伝わります。
24. 実験環境の情報が不足しています。 MicroK8sのバージョン，Kubernetesバージョン，OS，Linux kernel，CPU型番，メモリ量を記載できると再現性が大幅に向上します。
25. 「16コア」だけではCPU性能が分かりません。 同じ16コアでも性能差が大きいので，CPU型番は重要です。
26. k6のバージョンもあるとよいです。
27. k6の具体的な負荷生成方式を書いてください。 constant-arrival-rateなのか，VUs固定なのか等によって意味が異なります。
28. buy(x)，view(x)の定義は比較的分かりやすいですが，「1周」の処理内容をもう少し具体化できます。 特にbuyで何リクエスト発生するかが分かると，150周/秒という負荷量を解釈できます。
29. viewが到達するサービスにcartが含まれる理由を説明した方がよいです。 「閲覧のみ」と書いているので，読者によってはcartへのアクセスを不思議に思う可能性があります。
36. なぜReplica数を4にしたのか説明してください。 典型値なのか，実験機に収まる最大値なのかが分かりません。
38. 分離・front3・megaのrequests/limitsを表にすると非常に分かりやすくなります。 現在は実験の公平性を読者が判断しにくい状態です。
41. 可能であればネットワーク転送量やpacket数も併記すると説得力が上がります。 softirqだけでなく，bytes/sec等と相関していることを示せるとよいです。
42. localhost化によってsoftirqが減少することと，ユーザ体感性能が改善することは別です。 p95/p99 latencyも示せると非常に強くなります。
43. 予備実験の結果で17–23%，43–51%と範囲を示していますが，ばらつきの意味が分かりません。 各負荷レートでの値なのか，繰返し実験のmin–maxなのかを明示してください。
44. buy(450)について分離だけ359.6±22.9があり，同居側は「約440」なのは統一感がありません。 すべて平均±標準偏差で示す方がよいです。
45. 3サイクルと6サイクルに分けた理由を説明してください。 基本帯3回，飽和帯6回と回数が異なるので，実験計画上の理由が必要です。
46. 「サイクル」の定義も必要です。 1サイクルが何秒なのか，150→200→250→300と連続して負荷を変えるのか等が分かりません。
48. 負荷を与える順序を固定した場合，履歴効果があります。 可能なら順番をランダム化したか，毎回初期化したかを書いてください。
49. 「取りこぼし」と「失敗」の定義をしてください。 k6のdrop iteration，HTTP error，timeout等，何を指すのか明確にすべきです。
50. 「目標レートを達成」という判定基準も必要です。 例えば99%以上達成なら成功など，定量的にするとよいです。
51. 本実験のrequests決定方法は面白いですが，少し説明不足です。 CPU使用量=c0+c1×負荷とした回帰式そのものを示すと理解しやすくなります。
52. 一次近似が妥当だったことを示してください。 決定係数R²などがあると，「負荷とCPU使用量を一次式で近似」という仮定を裏付けられます。
53. 6段階のbuy負荷が具体的に何だったのかを記載してください。
54. なぜ基準負荷をbuy(100)にしたのか説明してください。
56. **「適正化したサービスは同じ負荷で閾値に達し，必要台数が揃う想定」は重要な仮定です。** 「想定」で終わらず，実際に揃ったかを確認した結果を示すと強くなります。
57. requests調整が実CPU必要量に対して妥当かも評価するとよいです。 HPAを合わせるためだけにrequestsを操作すると，本来のrequests設定の意味から外れる可能性があります。
58. 本実験で「6つの組み合わせ」をなぜ選んだか説明してください。 全45組（10サービスから2つ選ぶ場合）の中で，この6組を選んだ基準が必要です。
59. この選択は結論に直接関係します。 通信量が大・中・小になる組を意図的に選んだのであれば，そのことを明記してください。
60. 「5サイクル」のため1組につき4負荷×5回=20点だと思われますが，本文で明示してください。
62. 総当たり探索の方法を具体的に記載してください。 requestsを何%から何%まで，何%刻みで変えたのかが必要です。
63. どちらのサービスのrequestsを変更したのかも明記してください。 片方だけなのか，両方なのかで意味が異なります。
64. 「解」の定義が曖昧です。 20点すべてでΔ=0になるrequests設定を「解」としているのであれば，そのように定義してください。
65. 同じ20点を使ってrequestsを最適化し，その20点で損が消えるかを評価しているのであれば，過学習に近い問題があります。 つまり，「観測した負荷パターンに合わせ込める」ことを示しているだけで，未知負荷でも有効とは限りません。
66. 可能ならrequestsを決めるデータと，評価するデータを分けるとよいです。 例えば3サイクルで調整して残り2サイクルで検証するだけでも，主張が強くなります。
67. 5～9%と28～48%の差は非常に重要なので，表または図にした方がよいです。 現在は文章に埋もれています。
68. 「28～48%動かせば実態から乖離する」という判断基準が主観的です。 何%以上なら許容できないのか，あるいはその変更によって実際のCPU利用率がどうなるかを示す必要があります。
71. 表1はこの論文の中心的な結果なので，より丁寧な説明が必要です。 現在の1>，4<は慣れるまで読み取りにくいです。
72. n<，n>という表記はサービスの左右関係と結びついていて認知負荷があります。 「L→R」「R→L」や「左サービス余剰」「右サービス余剰」のような表記も検討できます。
73. frontend+cartだけ分母が19なのは必ず説明してください。 他が20点なのにここだけ19点であり，読者は測定失敗なのか欠損なのか疑問に思います。
74. 欠損値があるなら，なぜ欠損したかも記載してください。
76. 「向きが反転」の定義を本文中で初めて出す際に明示してください。 負荷条件によってA>BとB>Aが両方観測されること，と定義すると理解しやすいです。
77. できれば「必要Replica数差」を負荷に対してグラフ化すると，反転が一目で分かります。 表よりもこの研究の新規性を視覚的に表現できます。
78. 例えば横軸view負荷，縦軸n(A)-n(B)として，0を跨ぐ組合せを示す図が非常に効果的です。 2ページなので表1をこのグラフに置き換える価値があります。
81. これは論文の重要な技術的論点です。 「同居による通信負荷削減がHPAのスケーリング判定に与える影響」は，本研究のモデルでは切り離しているのか，実測値に既に含まれているのかを明示した方がよいです。
84. **5章に突然出てくる20.7%，5.3%，3.9%，3.1%，2.2%が何の割合なのか分かりにくいです。** 「通信量順」とありますが，何に対する割合か，どの実験から算出したかを明記してください。 ★一部対応: 数値を results-edge-pairs.csv からの実測 affinity 割合(38.8/4.8/4.1/2.5/1.7%)に差し替え済(Python 再計算)。「サービス間通信に占める割合」の定義文は行数都合で本文から削除
85. この数値は重要なので，表1に「通信割合」列を追加してもよいです。 そうすると「通信量」と「反転有無」を同じ表で比較できます。
86. frontend+emailの通信量が考察に記載されていません。 6組実験したなら，6組すべてを示した方がよいです。
89. 単一ノード実験であることは大きな制約です。 本文でも認識されています。 これは「今後の課題」だけでなく「本研究の制約」として考察で明示してよいです。
90. 単一ノードではネットワーク通信が同一ホスト内で完結するため，複数ノードのKubernetesクラスタとは通信コスト構造が異なります。 したがって「Kubernetes一般」に結果を広げすぎない方がよいです。
91. CNIプラグインも記載するとよいです。 ネットワークスタックのオーバヘッドを評価する研究では重要な条件です。
92. 複数ノードにした場合，Pod配置が別ノードか同一ノードかによって通信コストが変わります。 今後の課題として「ノード間通信を含む配置条件」まで書くと具体的です。
93. 同居には通信以外の副作用もあります。 同一Pod化するとスケーリングだけでなく，障害分離，デプロイ単位，ライフサイクル，リソース競合なども変化します。本研究の対象外と一言明記しておくとよいです。
94. 特に全サービス同居のmegaは実運用上かなり極端な設定です。 予備実験で上限効果を見るための構成であることを明示すると誤解されにくくなります。
95. 「megaが実用的」という印象を与えないようにしてください。 「通信削減効果の上限を観測するための比較構成」と位置付けるとよいです。
96. 本実験で同居したのが2サービス単位なのかを明確にしてください。 予備実験のfront3・megaと，本実験のペア同居とで実験構成が変わっています。
97. その変化を4章冒頭で説明すると読みやすくなります。 「以降は同居相手選択を検証するため2サービスのペアを対象とする」と一文入れるだけでも違います。
98. 結果に統計的なばらつきがほとんど示されていません。 システム実験なので必ずしも有意差検定は必要ありませんが，少なくとも平均・標準偏差またはmin/maxを統一して示すとよいです。
99. 5回測定しているなら，Replica数が何回その状態になったかだけでなく，平均Replica数や分布を示す方法もあります。
100. 表1は「食い違った回数」だけなので，差が1Replicaなのか3Replicaなのかが分かりません。 粒度損の大きさを評価するなら，Replica数差の大きさも重要です。
101. 「食い違いの有無」と「粒度損の量」は別です。 1回の大きな食い違いと5回の小さな食い違いでは意味が異なるので，Δの総量または平均値も示すとよいです。
102. 研究の中心指標として式(1)のΔを定義したのに，結果表でΔそのものがほとんど提示されていないのは少し惜しいです。 実験結果に平均Δや最大Δを示すと，式を導入した意味が強くなります。
103. むしろ表1を「反転有無＋平均粒度損＋通信量」の3項目にすると，論文全体が非常にまとまりやすくなります。
105. 「便益」という言葉も同様です。 本研究で直接測っている便益はsoftirq削減と高負荷時のthroughput向上です。この2つを区別した方がよいです。
106. buy(450)のthroughput向上は興味深いため，softirq削減との因果関係を断定しないようにしてください。 同居に伴う他の要因もあり得ます。「通信処理削減が寄与したと考えられる」程度が安全です。
109. 英語所属表記が日本語と英語の混在になっています。 前の論文と同様，Mizuki Tao, Graduate School of Systems Engineering, Wakayama Universityのように統一した方がきれいです。
110. 本文の「front3」「mega」は分かりやすい一方，初出時に「以下，front3と呼ぶ」などと明確に定義してください。
112. 図が1つもない点は少し惜しいです。 特に「分離配置」と「同居配置」を示す簡単な構成図があると，非専門読者の理解が大きく改善します。
113. 2ページ制限なら，式(1)の具体例を兼ねた図を1つ入れる価値があります。 A=4台，B=2台→同居4Pod→Bが2コンテナ余剰，という図です。
114. さらに重要なのは「反転」を示す図です。 ページに余裕がなければ，構成図よりこちらを優先してもよいと思います。
115. 「今後の課題」だけで終わるのではなく，1～2文の「おわりに」を入れた方が論文として締まります。 現在は複数ノード検証の必要性で終了しています。
116. 結論では具体的な成果を数字で再掲するとよいです。 「複数Replica環境でもsoftirqを17～51%削減」「必要台数の向きが反転する3組ではrequests調整量が28～48%必要」とまとめると明快です。
117. ただし数字の一般化は避け，「Online Boutiqueを用いた本実験では」と限定してください。
118. 研究上の貢献を最後に明示してください。 例えば「HPA環境における同居では，通信量だけでなく負荷変化に伴う必要Replica数の相対関係を考慮する必要があることを示した」が，現状最も説得力のある貢献です。
119. 本論文では「最適な同居相手を自動選択する手法」までは提案していません。 したがって「判断軸を示す」という現状の表現は適切で，ここを過度に「選択手法」と強めない方がよいです。
120. 逆に今後は，通信量と粒度損を組み合わせたスコアによる自動選択まで進めると，次の研究として非常に自然です。 現研究で「判断軸」，次研究で「配置アルゴリズム」という流れが作れます。
この中でも，まず直してもらうなら，優先順位は次のようになります。
優先度   修正点
済 最優先   N=max n(s)と実際のHPA制御の関係を明確化
済 最優先   HPA設定（CPU，70%，min/max replicas等）の記載
済 最優先   requests，「予約枠」，粒度損の定義整理
済 最優先   同居時にlocalhost通信をどう実現したかの説明
済 最優先   「softirq＝通信量」という表現の修正
高 requests総当たり探索の方法と「解」の定義
済 高 28～48%なら「実態から乖離」とする根拠
高 表1の19/20の理由，通信量%の定義
済 高 「便益と損は独立」という主張の弱め
高 6組を選択した理由
中 実験環境・バージョン等の再現性情報
中 関連研究の追加
中 反転を示すグラフの追加
中 結論節の追加
特にこの論文は，「通信量の大きいサービス同士を同居させればよい」という直感に対して，「HPAを考えるとそれだけでは不十分で，負荷変化に対するReplica数の相対関係を見る必要がある」と示しているところを研究の中心に据えると，かなり論文らしくなります。現在もその結論自体は書かれていますが，前半では「Kubernetesでも同居すると通信コストが下がる」という話が比較的大きく見えるため，新規性の中心がやや隠れています。
タイトルもその方向なら，「Kubernetes HPA環境におけるマイクロサービス同居の粒度損と同居相手選択」，あるいは少し自然に 「Kubernetes HPA環境におけるマイクロサービス同居の効果と選択条件」 が，現在の内容にはかなり合っていると思います



### 対応済み (2026-09-14, TeX 反映済)

1. タイトルの「適用」が曖昧です。 現在の研究は「同居を適用した」ことより，「HPA環境で同居した際の便益と粒度損を分析し，同居相手の選択条件を検討した」研究です。例えば「Kubernetes HPA環境におけるマイクロサービス同居の効果と適用条件」や「Kubernetes HPA環境におけるコンテナ同居の粒度損分析」の方が内容を正確に表します。
2. 概要の「通信コスト」という表現が広すぎます。 実際に測っているのはsoftirqのCPU時間なので，「通信処理に伴うCPUオーバヘッド」と書く方が正確です。概要では「通信コストを下げる」と一般化されています。
3. 「予約枠」という用語はKubernetesのrequestsと完全には同義ではないため注意が必要です。 KubernetesのrequestsはスケジューリングやHPAのCPU利用率計算に用いられる資源要求量であり，CPUがその量だけ排他的に予約されるという意味ではありません。「予約枠」を独自用語として使うなら，最初に明確に定義した方がよいです。
4. 「粒度損」は良い独自用語ですが，何の損なのかをもう少し明確にしてください。 式(1)がCPU requestsだけを対象としているなら，「CPU requestsに基づく余剰確保量」などと明記する方がよいです。
5. 式(1)の単位を明記するとよいです。 r(s)がCPU requestならmillicore等の単位になり，ΔもCPU量になります。「資源」と一般化すると，メモリ等まで含むように読めます。
## 6. N=max_s n(s)は「実際のHPAが決定するPod数」ではなく，「すべてのサービスの必要台数を満たすために必要となるPod数」というモデル上の値として定義した方が安全です。 現状は「その台数は各コンテナが必要とする台数の最大値になる」と断定しています。
## 7. この点は技術的に最重要です。 HPAは単に各コンテナの必要台数の最大を取る仕組みではありません。どのメトリクスをHPAに指定したかによってReplica数の決まり方が変わります。そのため，「概念モデル」と「実際のHPA制御」を分離して説明すべきです。
## 8. HPAで何のメトリクスを使ったのかを書いてください。 CPU使用率なのか，CPU使用量なのか，Custom Metricsなのかが本文から分かりません。
## 9. HPAのtarget utilizationを明示してください。 本実験の式から0.7，つまり70%と推測できますが，読者に推測させるべきではありません。
## 10. HPAのminReplicas，maxReplicasを記載してください。 実験結果を再現するために必須です。
## 11. HPAのscale-up/scale-down設定も必要です。 stabilization windowやscale behaviorによって，負荷変化時のReplica数は大きく変わります。
12. HPAの同期周期や負荷を何秒間与えたのかも重要です。 短時間の測定だとHPAが追従し終わっていない可能性があります。
13. 「必要台数」の定義をもっと厳密にしてください。 HPAが最終的に到達したReplica数なのか，要求負荷を取りこぼさず処理できる最小Replica数なのか，CPU使用率から理論計算した値なのかが現在は不明です。
15. この意味で式(1)はもう少し丁寧な説明が必要です。 「あるrequests設定のもとで単独配置した場合にHPAが要求するReplica数をn(s)とする」と定義すると理解しやすくなります。
21. 「実運用のデファクトスタンダードであるKubernetes」は根拠が必要です。 表現を弱めて「広く利用されているKubernetes」程度にするか，利用状況を示す文献を付けるのがよいです。
23. Online Boutiqueのバージョンまたはコミットを記載してください。 リポジトリは更新されるため，「2025」だけでは再現性が十分ではありません。
30. 同居をどのように実装したかが不足しています。 front3でfrontend，recommendation，catalogを一つのPodに配置しただけなのか，通信先もlocalhostに変更したのかを明記してください。
31. ここもかなり重要です。 複数Replicaが存在するとき，「同じPod内の相手」に確実に通信させる方法を説明する必要があります。通常のService経由だと，他PodのReplicaへ通信する可能性があります。localhost化の具体的方法が必要です。
32. mega構成も同様です。 10サービスすべてを一つのPodにした際，各サービス間通信をどのようにlocalhostに切り替えたのかを説明してください。
33. Podへの同居以外の変更がないことを確認できる記述が欲しいです。 アプリケーションコードの変更，Service定義，環境変数等を変えている場合，性能差の要因になり得ます。
34. 予備実験は「HPA環境での実験」と呼ぶには少し注意が必要です。 HPAを切ってReplica数を4に固定しているため，実際には「スケールアウト済み状態を模擬した固定Replica実験」です。
35. このため「HPA環境での効果検証」ではなく，「複数Replica環境で同居効果が維持されるかの予備検証」とする方が正確です。
37. 「利用できる資源は3構成とも一律」の具体的な意味を示してください。 CPU limitsなのかrequestsなのか，ノード全体の割当なのかが不明です。
39. softirqを通信量の代理指標とする根拠をもう少し説明すべきです。 softirq CPU時間にはネットワーク処理以外の要素も含まれ得るため，「通信量」と直接同一視しない方が安全です。
40. **「通信量としてsoftirqのCPU時間を用いた」は表現として特に注意が必要です。** 「ネットワークスタック処理負荷の代理指標としてsoftirq CPU時間を用いた」とする方が適切です。
47. ウォームアップ時間を設けたかも記載した方がよいです。 Java等を含むシステムでは起動直後と定常時で性能が異なる可能性があります。
55. (c0+100c1)/0.7の0.7がHPA閾値70%であることを式の直後に明記してください。
61. 「全20点で損を消せる予約枠を総当たりで探す」という文は，一読では6組全体で20点のようにも読めます。 「各組合せについて20測定点を対象に」としてください。
69. requestsは「実測した必要量そのもの」ではありません。 HPA目標値を含んだ設定値なので，「実態から乖離」という言葉は少し慎重に使った方がよいです。
70. **「事実上予約枠では消せない」も少し強い表現です。** 「実測値に近いrequests設定を維持したまま解消することは困難であった」とする方が実験結果に忠実です。
75. 「上3つ」「下3つ」という説明は表の並び順に依存するため，少し弱いです。 「反転が観測されなかった3組」「反転が観測された3組」と書く方が明確です。
79. **考察の「便益は通信量で決まり，損は必要台数の向きが反転するかで決まる」は言い切りすぎです。** 本実験で確認した範囲では，という限定を入れるべきです。
80. さらに「両者は独立」という主張には注意が必要です。 同居によって通信CPU負荷が減れば，CPU利用率を使うHPAのReplica数にも影響する可能性があり，通信便益とスケーリング挙動が完全に独立とは限りません。
82. 「避けるべきは通信量が多く，かつ向きが反転する組み合わせ」という結論も再検討した方がよいです。 通信量が多ければ便益も大きいので，「避けるべき」というより「便益と粒度損のトレードオフを評価すべき」とする方が自然です。
83. むしろ結果からは「通信量が多い相手を選ぶだけでは不十分」という結論が最も強く支持されています。 これは本文後半の主張とも一致します。
87. 「通信量だけで相手を選ぶ方針では不十分」という結論は本論文の重要な成果なので，概要にも明確に入れるとよいです。 現在の概要ではやや抽象的です。
88. 逆に，「必要台数の大小が入れ替わる相手を選ぶと予約枠を調整しても損が残る」という概要表現は，実験範囲に限定した方がよいです。 「本実験では～ことを確認した」とすると安全です。
104. 本文では「損」という言葉が頻繁に出ますが，何を損失としているのかを一貫させてください。 資源要求量の余剰であり，性能損失や金銭コストとは異なります。
107. 英語タイトルにKubernetes/HPAが入っていません。 日本語タイトルとの対応を考えると，Microservice Container Colocation under Kubernetes HPA などの方が内容が伝わります。
108. 英語タイトルでAutoscalingだけでは対象が広すぎます。
111. productcatalog，recoなど略称も表記を統一してください。 Online Boutiqueの正式なサービス名がproductcatalogservice等であれば，正式名と略称を最初に整理するとよいです。

## ビルド

```sh
cd FOSE2026-TeX-UTF8
platex -kanji=utf-8 fose2026; pbibtex fose2026; platex -kanji=utf-8 fose2026; platex -kanji=utf-8 fose2026; dvipdfmx fose2026.dvi
```

`latexmk` はこの TeXLive に未インストール。**必ず `FOSE2026-TeX-UTF8/` 内で実行**(リポジトリルートで実行すると `texput.log` を残して失敗する)。
